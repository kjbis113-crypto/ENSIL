# 전자생물 아카이브 — 웹 기획 / 와이어프레임 계획

> 문서 버전 v0.2 · 2026-08-08
> v0.1에서 변경: 그리드 인덱스/상세 분리 구조 폐기 → **상단 목차 + 하단 대형 뷰 단일 화면**으로 재설계.
> 피지컬 컴퓨팅 연동을 별도 계층(§7)으로 추가.
>
> 이 단계의 목표는 **작동하는 로우 피델리티 와이어프레임**입니다. 색·타이포·모션은 전부 나중 일.

---

## 0. 이번 단계의 범위

| 한다 | 안 한다 |
|---|---|
| 화면 구조(레이아웃) 확정 | 색상 팔레트, 브랜드 적용 |
| 목차 ↔ 대형 뷰 전환 흐름 | 폰트 선정, 타이포 스케일 |
| 주석(콜아웃) 시스템의 뼈대 | 주석 선의 곡률·애니메이션 |
| 데이터 스키마 확정 | 실제 전자생물 이미지 리터칭 |
| 시뮬레이션 규칙의 뼈대 | 파티클, 셰이더, 3D |
| 피지컬 입력 **인터페이스**와 목업(키보드) | 실제 하드웨어 회로 |
| Vite + Vercel 배포 파이프라인 | 도메인, OG 이미지, SEO |

**판단 기준 한 줄:** "이 화면을 회색 상자로만 그렸을 때도 무슨 화면인지 알 수 있는가?" → 예면 통과.

### 전시 UX 3원칙 (모든 화면 결정에 우선한다)

1. **노 스크롤** — 전시 모니터에서 스크롤은 없다. 모든 정보는 한 화면 안에서 해결. 넘치면 "펼치기"가 아니라 정보를 줄인다. (유일한 예외: 소개 오버레이 내부)
2. **뎁스 1** — 어떤 정보든 최대 1클릭. 페이지 이동 대신 같은 화면 안에서의 상태 전환. 소개(/about)도 별도 페이지가 아니라 **오버레이**로 연다.
3. **클릭 최소** — 목차 클릭 한 번 = 표본 전체 정보가 다 뜸. "더 보기", 탭, 아코디언 금지.

### 타깃 디스플레이

**24인치 iMac** (4.5K Retina, 논리 해상도 **2240×1260**, 약 16:9).
- 기준 설계 폭 2240px. 이 기준에서 여백·패널 배치를 잡는다.
- 개발·공유용 폴백: 1280~1512px 노트북에서도 성립해야 함 (같은 구조, 밀도만 축소).
- 1280px 미만은 전시 대상이 아니므로 1단 세로 폴백으로만 대응 (깨지지 않는 수준).

---

## 1. 프로젝트 개요

가상 생명체 탄생 메커니즘 컨셉에서 파생된 **'전자생물(Electro-organism)'** 을 다루는 웹.
네 개의 축으로 구성한다.

1. **아카이브(Archive)** — 각 전자생물의 비주얼을 표본처럼 수집·전시
2. **생태(Ecology)** — 개체별 서식 환경, 에너지원, 증식 방식, 관찰 기록
3. **시뮬레이션(Simulation)** — 각 개체에 부여된 *최초의 목적(Primary Drive)* 을 심즈처럼 단순 그래픽으로 구동
4. **물리 연동(Physical)** — 피지컬 컴퓨팅 목업의 실물 조작이 웹 화면에 실시간 반영

전시 현장에서는 오프라인 로컬 실행. 온라인에서는 Vercel 퍼블리싱.

### 레퍼런스 해석 — sutera.ch에서 가져올 것 / 버릴 것

| 가져온다 | 버린다 |
|---|---|
| 대상(표본)을 화면 중앙에 **고립**시키고 여백을 크게 | 스크롤 내러티브 (우리는 스크롤 안 함) |
| 대상 각 부위에서 뻗어나오는 **주석선 + 라벨** | 로딩 연출, GIF, 이스터에그 |
| 화면 가장자리에 **떠 있는 정보 패널** (문서 흐름 아님) | 포트폴리오식 프로젝트 나열 |
| 모서리의 **시스템 메타데이터** (시각, 상태) | 개인 서사 톤 |
| 작은 모노스페이스 대문자 마이크로 라벨 | |

**핵심 차용점:** 웹페이지가 아니라 **관측 장비의 화면**처럼 보이게 한다.
이 톤은 로우파이 와이어프레임과 궁합이 좋다 — 회색 선과 상자만으로도 성립하기 때문.

---

## 2. 확정된 기술 결정

| 항목 | 결정 | 사유 |
|---|---|---|
| 빌드 | **Vite** (빌드는 Node 20/22 — Node 24 크래시, debug.md #1) | 요구사항. 로컬 `preview` 로 오프라인 전시 대응이 쉬움 |
| 프레임워크 | **React + TypeScript** | 목차/뷰/시뮬/물리입력이 하나의 상태를 공유. 스키마를 타입으로 고정하면 이후 리팩토링 안전 |
| 라우팅 | **자체 해시 훅** (`useHashRoute`, react-router 미사용) | 라우트가 2개뿐 — 상태의 주소일 뿐이라 훅 하나로 충분. 의존성 제거 |
| 시뮬 렌더 | **DOM + CSS transform** | 디버깅과 개체 클릭 처리가 쉬움. 단, 아래 *경고* 참조 |
| 주석 선 | **인라인 SVG 오버레이** | 좌표 기반 선/점을 그리기엔 SVG가 정답. 이미지 위에 절대배치 |
| 데이터 | **로컬 JSON 우선 + Supabase 동기화** | 와이파이 없어도 100% 동작 보장 |
| 물리 연동 | **로컬 WebSocket 브릿지** | §7 참조. 오프라인에서 동작해야 하므로 Supabase Realtime 배제 |
| 언어 | **한국어 단일** | 다만 하드코딩 금지 (§10 참조) |
| 배포 | **Vercel** (git push 기준 자동) | |
| 버전관리 | **Git** (브랜치 전략 §12) | |

> ⚠️ **DOM 렌더의 한계와 대비책**
> DOM 노드는 개체 60~80마리를 넘어가면 프레임이 떨어진다. 그래서 시뮬레이션은 처음부터
> **"상태 계산(엔진) ↔ 그리기(렌더러)"를 분리**해서 만든다. 엔진은 순수 TS로 좌표 배열만 뱉고,
> 렌더러는 그 배열을 받아 그린다. 나중에 개체가 많아지면 `DomRenderer` → `CanvasRenderer` 로
> **파일 하나만 갈아끼우면 되도록** 한다. 이 분리는 이번 단계에서 반드시 지킨다.

---

## 3. 정보 구조 (IA) & 라우트

**페이지를 옮겨다니는 웹이 아니다.** 하나의 화면 안에서 상단 목차로 대상을 바꾸는 구조.
따라서 라우트는 사실상 **상태의 주소일 뿐**이다.

```
/                     메인 — 목차 + 대형 뷰 (미선택 = 전체 개괄 상태)
/c/:id                메인 — 해당 개체 선택됨            ← 실질적 주 화면
/c/:id?mode=sim       메인 — 해당 개체, 시뮬레이션 모드
/?about=1             소개 오버레이 (페이지 아님 — 메인 위에 덮임, Esc/× 로 닫기)
```

라우트를 나눈 이유는 딱 두 개: **새로고침해도 선택이 유지될 것**, **전시 현장에서 특정 개체로
바로 띄울 수 있을 것**. 그 외 목적으로 라우트를 늘리지 않는다.

구현은 **HashRouter** (`#/c/:id`) — 전시장 로컬 서버에서 서버 설정 없이 새로고침이 안 깨진다.

**동선**
```
   ┌─────────────── 상단 목차 (항상 보임) ───────────────┐
   │  EO-001  EO-002  EO-003  EO-004  ...              │
   └───────┬───────────┬───────────┬───────────────────┘
           │           │           │        ← 클릭 / 물리 입력 둘 다 여기로 들어옴
           ▼           ▼           ▼
   ┌──────────────────────────────────────────────────┐
   │  대형 뷰:  [표본 모드] ⇄ [시뮬레이션 모드]           │
   └──────────────────────────────────────────────────┘
```

---

## 4. 메인 화면 와이어프레임

### 4-1. 전체 골격

```
┌────────────────────────────────────────────────────────────────────────────┐
│ 전자생물 아카이브                    ◍ 물리연동 대기          2026-08-08 14:22 │ ← 시스템 바 32px
├────────────────────────────────────────────────────────────────────────────┤
│ ┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐             │
│ │ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  │  →         │ ← 목차 스트립
│ └────┘└────┘└────┘└────┘└────┘└────┘└────┘└────┘└────┘└────┘             │   120px
│  001   002  [003]  004    005   006   007   008   009   010                │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│                          < 대 형   뷰 >                                     │
│                                                                            │
│                        (남는 높이 전부)                                      │
│                                                                            │
├────────────────────────────────────────────────────────────────────────────┤
│ [표본] [시뮬레이션]                              소개    EO-003 / 24        │ ← 모드 바 48px
└────────────────────────────────────────────────────────────────────────────┘
```

- **시스템 바** — 좌: 제목 / 중: 물리연동·동기화 상태 / 우: 현재 시각. 레퍼런스의 `LOCAL TIME` 대응.
  전시장에서 "지금 잘 돌고 있나"를 한눈에 확인하는 용도이기도 하다.
- **목차 스트립** — 이 웹의 척추. §4-2.
- **대형 뷰** — 모드에 따라 내용이 바뀜. §4-3 / §4-4.
- **모드 바** — 모드 전환 + 소개 진입 + 현재 위치 표시. 하단에 둬서 상단 목차와 역할이 안 섞이게.

---

### 4-2. 상단 목차 스트립

```
┌────────────────────────────────────────────────────────────────────────────┐
│┌────┐┌────┐╔════╗┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐┌────┐         │
││ ▨  ││ ▨  ║ ▨  ║│ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  ││ ▨  │  ▸      │
│└────┘└────┘╚════╝└────┘└────┘└────┘└────┘└────┘└────┘└────┘└────┘         │
│ 001   002   003   004   005   006   007   008   009   010   011           │
│             ▲                                                              │
│         선택됨 (테두리 두껍게 + 코드 굵게)                                     │
└────────────────────────────────────────────────────────────────────────────┘
```

- 슬롯 하나 = 개체 하나. **80×80 정사각 썸네일 + 하단 3자리 코드.**
- 개체가 24종을 넘어가면 가로 스크롤. 스크롤바는 숨기고 우측 `▸` 로 더 있음을 표시.
- 선택 표시는 **테두리 두께**로만. 색으로 하지 않는다 (모노톤 원칙 + 나중에 색 입힐 때 자유도 확보).
- 키보드 `←/→` 로 이동. **이게 곧 물리 컨트롤러의 기본 매핑이 된다** (§7-2).
- 미선택 상태(`/`)에서는 전체가 동일 두께 → 대형 뷰에 개괄 화면.

> **왜 그리드가 아니라 스트립인가**
> 그리드 인덱스는 "고르는 화면"이고 스트립은 "훑는 목차"다. 관람객이 실물 목업을 만지면서
> 화면이 따라 바뀌는 구조에서는, 목차가 항상 보이면서 지금 어디쯤인지 알려줘야 한다.
> 그리드는 선택 후 사라져서 이 역할을 못 한다.

---

### 4-3. 대형 뷰 — **표본 모드** (기본, 레퍼런스 구조)

```
┌────────────────────────────────────────────────────────────────────────────┐
│                                                                            │
│  ┌──────────────────┐                              ┌────────────────────┐  │
│  │ EO-003           │                              │ ▸ 계통             │  │
│  │ ───────────────  │              □               │  전자생물          │  │
│  │ 개체명            │              │               │   └ 계통 B         │  │
│  │ Latin-ish name   │              │               │      └ EO-003     │  │
│  │ 등재 2026-07-12  │      ┌───────┘               └────────────────────┘  │
│  │ 상태 관찰 중      │      │                                               │
│  └──────────────────┘      │  ┌───────────────┐                            │
│                            └──┤               │──────────────┐             │
│                               │               │              │             │
│                               │   [ 표본      │              +             │
│                               │     비주얼 ]   │        ┌──────────────┐   │
│                               │               ├────+    │ 주석 02      │   │
│                               │               │    │    │ 에너지원과    │   │
│                               └───────────────┘    │    │ 대사에 관한   │   │
│                                    │               │    │ 짧은 서술     │   │
│                                    +          ┌────┴──┐ └──────────────┘   │
│                          ┌──────────────┐     │주석 03│                    │
│  ┌──────────────────┐    │ 주석 01      │     │ ...   │                    │
│  │ ▸ 최초의 목적     │    │ 서식 환경에   │     └───────┘                    │
│  │ ──────────────   │    │ 관한 짧은     │                                  │
│  │ "○○○을 향해      │    │ 서술          │    ┌───────────────────────────┐ │
│  │  이동한다"        │    └──────────────┘    │ ▸ 관찰 기록                │ │
│  │ ──────────────   │                        │ · 2026-06-29 기록 한 줄    │ │
│  │ 전하 ▮▮▮▮▮▮▯▯▯▯  │                        │ · 2026-07-12 기록 한 줄    │ │
│  │ 자극 ▮▮▮▯▯▯▯▯▯▯  │                        │ [ 전체 기록 펼치기 ]        │ │
│  │ 결속 ▮▮▮▮▮▮▮▮▯▯  │                        └───────────────────────────┘ │
│  └──────────────────┘                                                      │
└────────────────────────────────────────────────────────────────────────────┘
```

구성 요소는 **네 종류뿐**이다. 이 네 개로 모든 개체 페이지가 만들어진다.

| 요소 | 역할 | 배치 |
|---|---|---|
| **표본 비주얼** | 중앙 고립. 화면 높이의 55~65% | 정중앙 (약간 위) |
| **정보 패널** | 제목 / 계통 / 목적 / 관찰기록 — 4개 고정 슬롯 | 네 모서리 영역에 절대배치 |
| **주석(콜아웃)** | 비주얼의 특정 지점 → 선 → 라벨 박스. 생태 설명이 여기 들어감 | 데이터의 좌표를 따름 |
| **마커** | `+` `□` — 주석의 시작점 표시 | 비주얼 위 |

**주석은 하드코딩하지 않는다.** 각 개체 데이터가 자기 주석의 앵커 좌표(이미지 기준 0~1 비율)를
들고 있고, 컴포넌트는 그걸 읽어서 선을 그린다 (§6 `annotations`). 개체마다 부위가 다르므로
이건 반드시 데이터여야 한다.

**주석선 규칙 (로우파이 단계)**
- 직선만. 곡선·베지어 금지.
- 꺾임은 최대 1회 (수직 → 수평, 또는 그 반대).
- 라벨 박스는 좌우 가장자리 쪽으로만 나감. 중앙 침범 금지.

**반응형** — 1280px 미만에서는 떠 있는 패널 구조가 깨진다.
→ 표본 비주얼 아래로 패널들이 **1단 세로 나열**되는 폴백 레이아웃을 만든다.
전시는 대형 화면이지만 개발·공유 시 노트북에서 봐야 하므로 필수.

---

### 4-4. 대형 뷰 — **시뮬레이션 모드**

같은 화면 골격(시스템 바 / 목차 / 모드 바)을 유지하고 대형 뷰의 내용만 교체한다.
목차가 그대로 있으므로 시뮬레이션 중에도 다른 개체로 넘어갈 수 있다.

```
┌────────────────────────────────────────────────────────────┬───────────────┐
│                                                            │ 관찰 패널      │
│    ○         □                    ○                        │ ──────────    │
│                                                            │ ┌───────────┐ │
│         ○                   △            ○                 │ │ [비주얼]   │ │
│                    ⊙ ← 에너지 노드                           │ └───────────┘ │
│                                          □                 │ EO-003        │
│              ○        ○                                    │ 개체명         │
│                                    ⊙                       │               │
│         △                                                  │ 상태  탐색     │
│                    ○         □                             │ 전하  ▮▮▮▯▯   │
│                                                            │ 자극  ▮▮▯▯▯   │
│    ⊙                              ○                        │ 결속  ▮▮▮▮▯   │
│                                                            │               │
│              ○                              □              │ 목적           │
│                                                            │ "○○○"        │
│                                                            │               │
├────────────────────────────────────────────────────────────┤ [표본 보기 ›] │
│ [▶/❚❚] [↺]  속도 ×0.5 ×1 ×2 ×4   t=00:03:12   개체 12      │               │
│ 표시: [☑궤적] [☑상태라벨] [☐노드] [☐그리드]                    │               │
└────────────────────────────────────────────────────────────┴───────────────┘
```

- 개체는 도형만: `○` 원, `□` 사각, `△` 삼각 — 계통별 형태 구분. 크기는 전하량 비례.
- 선택된 개체는 테두리 두껍게 + 나머지는 흐리게.
- 개체 클릭 → 우측 패널 교체 + **상단 목차의 선택도 함께 이동** (두 곳의 선택 상태는 항상 하나).
- 하단 표시 토글은 디버깅 겸 관람 옵션. 전시장에서는 프리셋으로 고정.

---

### 4-5. 소개 오버레이 `/?about=1`

페이지가 아니라 메인 위에 덮이는 오버레이 (뎁스 1 원칙).

```
┌────────────────────────────────────────────────────────────┐
│ ░░░░░░░░░░░░ (메인 화면이 흐리게 비침) ░░░░░░░░░░░░░░░░░░░░ │
│ ░░┌────────────────────────────────────────────────┐░░░░░ │
│ ░░│  탄생 메커니즘                              [×]  │░░░░░ │
│ ░░│  ────────────────────────────────              │░░░░░ │
│ ░░│  (서술 텍스트 — 오버레이 내부만 스크롤 허용)        │░░░░░ │
│ ░░│  ┌──────────────────────────────┐              │░░░░░ │
│ ░░│  │    [ 메커니즘 다이어그램 ]      │              │░░░░░ │
│ ░░│  └──────────────────────────────┘              │░░░░░ │
│ ░░└────────────────────────────────────────────────┘░░░░░ │
└────────────────────────────────────────────────────────────┘
```

`Esc` 또는 `×` 로 닫힘. 사이트 전체에서 스크롤이 존재하는 유일한 곳.

---

## 5. 화면 상태 모델

메인 화면 전체가 아래 상태 하나로 기술된다. **목차 클릭 / 시뮬 개체 클릭 / 물리 입력 /
URL 진입 — 네 개의 입구가 전부 이 상태 하나를 바꾼다.** 입구마다 로직을 따로 쓰지 않는다.

```ts
interface ViewState {
  selectedId: string | null;      // null = 개괄 상태
  mode: 'specimen' | 'sim';
  simRunning: boolean;
  simSpeed: 0.5 | 1 | 2 | 4;
  overlays: { trails: boolean; labels: boolean; nodes: boolean; grid: boolean };
  physical: { connected: boolean; lastSignalAt: number | null };
}
```

`selectedId` 변경 → URL 동기화(`/c/:id`)까지가 한 세트. 이 규칙을 어기면
물리 입력으로 바뀐 화면이 새로고침에 날아간다.

---

## 6. 데이터 모델

`src/types/creature.ts` 에 타입으로 고정하고, `src/data/creatures.json` 이 이 타입을 따른다.
**Supabase 테이블 컬럼도 이 스키마와 1:1로 맞춘다.**

```ts
type Taxon = 'A' | 'B' | 'C';           // 계통 — 확정되면 실제 이름으로 교체
type Shape = 'circle' | 'square' | 'triangle';
type DriveKind = 'seek_energy' | 'seek_kin' | 'avoid_light' | 'expand' | 'persist';

interface Annotation {
  id: string;
  anchor: { x: number; y: number };   // 이미지 기준 0~1 비율 좌표
  side: 'left' | 'right';             // 라벨 박스가 나갈 방향
  label: string;                      // 짧은 제목 — "에너지원"
  body: string;                       // 2~4줄 서술
}

interface Creature {
  id: string;              // 'eo-003'
  code: string;            // 'EO-003'  표본번호
  name: string;            // 국문 개체명
  latin?: string;          // 학명 형식의 별칭
  taxon: Taxon;
  registeredAt: string;    // 'YYYY-MM-DD'
  status: '관찰 중' | '휴면' | '소실';

  visual: {
    main: string;          // '/creatures/eo-003/main.png'
    thumb: string;         // 목차 스트립용 정사각
    shape: Shape;          // 시뮬레이션에서 그려질 도형
  };

  annotations: Annotation[];   // 표본 모드의 콜아웃. 3~5개 권장

  ecology: {                   // 주석에 안 들어가는 긴 서술
    habitat: string;
    metabolism: string;
    reproduction: string;
    lifespan?: string;
  };

  // 심즈식 '최초의 목적'
  purpose: {
    kind: DriveKind;
    statement: string;     // "전위차가 큰 곳을 향해 이동한다"
  };

  // 0–10 정수. 시뮬레이션 파라미터 겸 표본 모드 게이지
  traits: {
    charge: number;        // 전하 — 에너지 보유/소모율
    stimulus: number;      // 자극 — 반응 민감도, 이동 속도
    bond: number;          // 결속 — 동종 개체와 뭉치는 정도
  };

  notes: { date: string; text: string }[];   // 관찰 기록

  physical?: {                 // 피지컬 컴퓨팅 목업과의 결선 (§7)
    slot: number;              // 목업의 몇 번 자리에 대응하는가
  };
}
```

**게이지 3개로 고정한 이유:** 심즈의 니즈 바처럼 읽히면서, 시뮬 규칙을 3개 변수로만
설명할 수 있어 로우파이 단계에서 튜닝이 가능하다. 늘리는 건 언제든 쉽고 줄이는 건 어렵다.

**`annotations` vs `ecology` 분리 이유:** 주석은 화면 위 좌표에 묶인 짧은 글이고,
`ecology`는 좌표와 무관한 긴 글이다. 섞으면 나중에 이미지를 교체할 때 텍스트까지 깨진다.

---

## 7. 피지컬 컴퓨팅 연동

### 7-1. 구조

브라우저가 하드웨어와 직접 대화하지 않는다. **로컬 브릿지 프로세스를 하나 둔다.**

```
 ┌──────────────┐   USB Serial   ┌───────────────┐   WebSocket    ┌──────────┐
 │  목업 하드웨어  │ ─────────────→ │  로컬 브릿지    │ ─────────────→ │   웹     │
 │ (Arduino 등)  │ ←───────────── │  (Node, ws)    │ ←───────────── │ (React)  │
 └──────────────┘                └───────────────┘                └──────────┘
                                   localhost:7777
```

**Web Serial API로 브라우저가 직접 읽는 방식을 쓰지 않는 이유**
- 크롬 전용이고, 포트 접근에 매번 **사용자 클릭 제스처**가 필요하다 → 키오스크 자동 실행과 충돌
- 화면을 2대 이상 띄우면 포트를 공유할 수 없다
- 브릿지를 두면 하드웨어가 바뀌어도 웹 코드는 그대로다

브릿지는 별도 폴더(`bridge/`)에 독립 Node 스크립트로 둔다. 웹 빌드와 무관하게 돌아간다.

### 7-2. 프로토콜 (JSON, 한 줄 = 한 메시지)

**하드웨어 → 웹**
```jsonc
{ "type": "select",  "slot": 3 }                          // 3번 자리 표본을 집었다/눌렀다
{ "type": "release", "slot": 3 }                          // 놓았다 → 개괄 상태로 복귀(옵션)
{ "type": "step",    "dir": -1 }                          // 다이얼/버튼: 목차 이전
{ "type": "sensor",  "channel": "voltage", "value": 0.62 } // 0~1 정규화. 시뮬 파라미터로 주입
{ "type": "trigger", "action": "pulse", "intensity": 0.8 } // 순간 이벤트
```

**웹 → 하드웨어** (LED·모터 피드백용, 양방향)
```jsonc
{ "type": "state", "selectedSlot": 3, "mode": "sim" }
```

**정규화 규칙:** 센서값은 **브릿지에서 0~1로 정규화해서 보낸다.** 웹은 아날로그 원값을 모른다.
센서를 바꿔도 웹 코드가 안 바뀌게 하는 장치.

### 7-3. 입력 추상화 (시뮬 렌더러 분리와 같은 원칙)

```
src/input/
  types.ts          ← InputSource 인터페이스, PhysicalEvent 유니온 타입
  MockInput.ts      ← 키보드로 위 이벤트를 흉내 (개발용, 기본값)
  WsInput.ts        ← localhost:7777 WebSocket (전시용)
  useInputSource.ts ← 위 둘 중 하나를 골라 ViewState에 연결
```

```ts
interface InputSource {
  connect(): void;
  disconnect(): void;
  onEvent(cb: (e: PhysicalEvent) => void): void;
  send(msg: OutboundMessage): void;   // 하드웨어 없으면 no-op
  readonly connected: boolean;
}
```

**하드웨어 없이 전체 인터랙션을 개발·검증할 수 있어야 한다.** `MockInput` 키 매핑:

| 키 | 이벤트 |
|---|---|
| `←` `→` | `step` |
| `1`~`9` | `select` (해당 슬롯) |
| `Esc` | `release` |
| `↑` `↓` | `sensor` voltage ±0.1 |
| `Space` | `trigger` pulse |

이 매핑은 전시 현장에서 하드웨어가 고장 났을 때의 **비상 조작 수단**이기도 하다. 반드시 남긴다.

### 7-4. 실패 대응 (가장 중요)

| 상황 | 동작 |
|---|---|
| 브릿지 미실행 | 3초마다 조용히 재연결 시도. 화면은 마우스·키보드로 100% 정상 동작 |
| 연결 끊김 | 시스템 바 상태 표시만 `◍ 물리연동 끊김`으로 변경. **모달·알림 금지** |
| 알 수 없는 슬롯 번호 | 무시하고 콘솔 경고만 |
| 신호 폭주 | 동일 타입 이벤트는 50ms 스로틀 |

**원칙: 하드웨어는 웹의 보조 입력이지 전제조건이 아니다.** 목업이 죽어도 전시는 계속된다.

---

## 8. 시뮬레이션 설계

### 8-1. 구조 (렌더러 분리 원칙)

```
src/sim/
  engine.ts       ← 순수 TS. DOM/React 모름. tick(state, dt) → nextState
  rules.ts        ← 목적(DriveKind)별 행동 함수
  world.ts        ← 월드 초기화, 에너지 노드 배치
  types.ts
src/components/simulation/
  DomRenderer.tsx ← state를 받아 <div>로 그림 (교체 대상)
  SimStage.tsx    ← 엔진 루프 + 렌더러 연결
```

`engine.ts`는 **React를 import 하지 않는다.** 이 규칙만 지키면 나중에 Canvas/Worker로
옮기는 비용이 거의 0이 된다.

### 8-2. 개체의 니즈 & 상태

각 개체는 3개 니즈를 갖고, 매 tick 감소한다.

| 니즈 | 감소 요인 | 회복 방법 |
|---|---|---|
| `energy` | 이동 시 `traits.charge` 반비례로 소모 | 에너지 노드 `⊙` 접촉 |
| `arousal` | 시간에 따라 감소 | 이동·다른 개체와 접촉 |
| `bonding` | 고립 시 감소 | 동종 개체 근접 |

**상태 머신** (가장 낮은 니즈가 상태를 결정)

```
       ┌──────┐  니즈 충족    ┌──────┐
       │ IDLE │ ───────────→ │ REST │
       └──┬───┘              └──────┘
          │ 니즈 하락
          ↓
       ┌──────┐  대상 도달   ┌─────────┐
       │ SEEK │ ──────────→ │ CONSUME │  또는  INTERACT
       └──────┘              └─────────┘
```

- `SEEK` 의 목표는 `purpose.kind` 가 결정한다 (에너지 노드 / 동종 개체 / 회피).
- 즉 **"최초의 목적"은 곧 SEEK 대상 선택 함수**다. 이게 심즈의 야망(Aspiration)에 대응.
- 표시: 개체 옆 라벨에 상태를 텍스트로 (`탐색` `섭취` `휴식` `접촉`).

### 8-3. 물리 입력의 시뮬 주입

`sensor` 이벤트는 **월드 전역 파라미터**로 들어간다. 개별 개체를 직접 조종하지 않는다.

```
{ sensor: voltage 0.62 }  →  world.ambientCharge = 0.62
                              ↳ 에너지 노드 회복 속도, 개체 이동 속도에 곱해짐
```

관람객이 목업을 조작하면 **생태계 전체의 분위기가 바뀌는** 형태.
개체를 직접 조종하는 것보다 "생명체를 관찰한다"는 컨셉에 맞고, 구현도 훨씬 단순하다.

### 8-4. 이번 단계에서 **안 하는 것**

번식으로 개체 수 증가, 사망, 진화/돌연변이, 개체 간 경쟁.
→ 규칙이 안정된 다음. 지금은 **개체 수 고정, 죽지 않음**.

---

## 9. 폴더 구조

```
Wireframe/
├─ plan.md                     ← 이 문서
├─ index.html
├─ package.json
├─ vite.config.ts              ← base: './' 필수
├─ tsconfig.json
├─ .env.example                ← VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY / VITE_BRIDGE_URL
├─ bridge/                     ← 웹 빌드와 분리된 독립 Node 스크립트
│  ├─ index.js                 ← serial ↔ websocket
│  └─ README.md                ← 배선·실행법
├─ public/
│  └─ creatures/               ← 개체 이미지 (빌드에 포함, 오프라인 대응)
├─ src/
│  ├─ main.tsx
│  ├─ App.tsx                  ← 라우터 + ViewState 프로바이더
│  ├─ copy.ts                  ← 모든 화면 문구
│  ├─ types/
│  │  ├─ creature.ts
│  │  ├─ view.ts               ← ViewState
│  │  └─ sim.ts
│  ├─ state/
│  │  └─ useViewState.ts       ← 단일 진실 공급원 + URL 동기화
│  ├─ data/
│  │  ├─ creatures.json
│  │  └─ loadCreatures.ts
│  ├─ lib/
│  │  └─ supabase.ts
│  ├─ input/                   ← §7-3
│  │  ├─ types.ts / MockInput.ts / WsInput.ts / useInputSource.ts
│  ├─ sim/                     ← 순수 로직 (React 금지)
│  │  ├─ engine.ts / rules.ts / world.ts / types.ts
│  ├─ cursor/
│  │  └─ useCursorTrail.ts     ← 포인터 샘플링·수명 관리 (표현과 분리)
│  ├─ routes/
│  │  ├─ Main.tsx              ← 시스템바 + 목차 + 대형뷰 + 모드바
│  │  └─ About.tsx
│  ├─ components/
│  │  ├─ shell/     (SystemBar, ModeBar, StatusDot)
│  │  ├─ cursor/    (CursorTrail — 와이어프레임: 회색 블롭. 본 디자인 때 이 파일만 교체)
│  │  ├─ index/     (IndexStrip, IndexSlot)
│  │  ├─ specimen/  (SpecimenView, AnnotationLayer, Callout,
│  │  │              InfoPanel, PurposePanel, TraitGauge, NotesPanel)
│  │  └─ simulation/(SimStage, DomRenderer, ObserverPanel, TransportBar)
│  └─ styles/
│     ├─ reset.css
│     ├─ tokens.css            ← 모노톤 변수만
│     └─ layout.css
```

**규칙:** `components/` 안의 것들은 데이터를 fetch 하지 않는다. props로만 받는다.
이걸 지키면 디자인 입힐 때 컴포넌트를 통째로 교체해도 안전하다.

---

## 10. 스타일 규칙 (로우파이 유지 장치)

`tokens.css` 에 아래 변수만 정의하고, **컴포넌트 CSS에서는 리터럴 색상값 사용 금지.**

```css
:root {
  --bg:        #ffffff;
  --surface:   #f2f2f2;
  --line:      #cccccc;
  --line-2:    #888888;
  --ink:       #111111;
  --ink-mute:  #777777;
  --accent:    #111111;   /* 지금은 검정. 나중에 여기만 바꾼다 */

  --sp-1: 4px; --sp-2: 8px; --sp-3: 16px; --sp-4: 24px; --sp-5: 40px;
  --radius: 0;            /* 로우파이 = 모서리 각짐 */
  --font: system-ui, sans-serif;
  --font-mono: ui-monospace, monospace;   /* 코드·라벨·시각 표시 전용 */
}
```

**금지 목록 (이번 단계)**
- 색상 (그레이스케일 외), 그림자, 그라디언트, 라운드 코너
- 커스텀 폰트, 애니메이션 (예외: 시뮬 개체 이동, 주석선 등장)
- 아이콘 이미지 → 텍스트나 유니코드 도형으로 대체

**텍스트 하드코딩 금지**
한국어 단일이지만, 화면 문구는 `src/copy.ts` 한 파일에 상수로 모은다.
번역 목적이 아니라 **문구를 한 곳에서 고치기 위해서**. i18n 라이브러리는 넣지 않는다.

---

## 11. Supabase 설계 & 오프라인 전략

### 11-1. 테이블

```sql
create table creatures (
  id text primary key,
  code text not null,
  name text not null,
  latin text,
  taxon text not null,
  registered_at date,
  status text,
  visual jsonb,        -- {main, thumb, shape}
  annotations jsonb,   -- [{id, anchor:{x,y}, side, label, body}]
  ecology jsonb,       -- {habitat, metabolism, reproduction, lifespan}
  purpose jsonb,       -- {kind, statement}
  traits jsonb,        -- {charge, stimulus, bond}
  notes jsonb,         -- [{date, text}]
  physical jsonb,      -- {slot}
  updated_at timestamptz default now()
);
```
- RLS: 익명 `select` 만 허용. 쓰기는 Supabase 대시보드에서 직접(초기엔 CMS 불필요).
- **이미지는 Supabase Storage에 두지 않는다.** `public/creatures/` 에 두고 빌드에 포함 →
  오프라인에서 이미지가 깨지지 않는다. Storage는 나중에 필요해지면 검토.

### 11-2. 로드 흐름

```
loadCreatures()
 ├─ 1. import local from 'data/creatures.json'   ← 항상 성공. 화면은 여기서 이미 그려짐
 ├─ 2. navigator.onLine === false → 종료
 ├─ 3. supabase.from('creatures').select()  (타임아웃 3초)
 │     ├─ 성공 → 로컬 위에 merge, localStorage에 캐시 + 타임스탬프
 │     └─ 실패/타임아웃 → 조용히 무시 (에러 화면 띄우지 않음)
 └─ 4. 시스템 바에 결과 표시 (온라인·최신 / 오프라인·로컬본)
```

핵심: **네트워크 실패가 절대 화면을 막지 않는다.** 전시 사고의 가장 흔한 원인이라 1순위로 처리.

---

## 12. 마일스톤

| # | 단계 | 산출물 | 완료 기준 |
|---|---|---|---|
| **M0** | 셋업 | Vite+React+TS, git init, Vercel 연결, `base:'./'` | `npm run dev` 로 빈 셸이 뜬다 |
| **M1** | 셸 골격 | SystemBar / IndexStrip(더미) / 대형뷰(빈 상자) / ModeBar | 4분할 골격이 화면에 꽉 참 |
| **M2** | 데이터 계층 | `creature.ts` 타입, 더미 8개 `creatures.json`, `loadCreatures` | 콘솔에 8개 개체가 타입 안전하게 찍힘 |
| **M3** | 목차 + 상태 | IndexStrip 실데이터, `useViewState`, URL 동기화, ←/→ 키 | 클릭·키보드로 선택이 바뀌고 새로고침해도 유지 |
| **M4** | **표본 모드** | 중앙 비주얼, 4개 정보 패널, AnnotationLayer(SVG 콜아웃) | 8개 개체 모두 주석선이 제 위치에 그려짐 |
| **M5** | 반응형 폴백 | 1280px 미만 1단 세로 레이아웃 | 노트북에서 깨지지 않음 |
| **M6** ✓ | 시뮬 엔진 | `sim/engine.ts` + 니즈/상태 머신 (UI 없이) | 콘솔에서 tick 돌려 좌표·상태가 변한다 |
| **M7** ✓ | 시뮬 모드 | DomRenderer, 관찰 패널, 트랜스포트 바 | 재생/정지/속도/선택 동작, 목차와 선택 동기화 |
| **M8** | **입력 추상화** | `input/` + MockInput(키보드) | 키보드만으로 물리 인터랙션 전부 재현 |
| **M9** | 브릿지 | `bridge/index.js`, WsInput, 상태 표시, 재연결 | 브릿지 껐다 켜도 웹이 안 죽음 |
| **M10** | Supabase | 테이블 생성, 동기화, 상태 표시 | 랜선 뽑고도 전 기능 동작 |
| **M11** | 전시 모드 | 로컬 실행 스크립트, 프리셋, 커서 숨김 | 전시용 PC에서 더블클릭 실행 |

**M5까지가 "메인 웹 레이아웃 와이어프레임 완성" 지점.** 여기서 한 번 끊고 검토.
M6~M7이 시뮬레이션 덩어리, M8~M9가 물리 연동 덩어리로 독립적이라 순서를 바꿔도 된다.

---

## 13. Git / 배포 워크플로

원격 저장소: **https://github.com/ystmk1/ENSIL.git** (연결 완료 — `origin/main` 추적 중)

```
main       ← Vercel Production 자동 배포. 항상 동작하는 상태 유지
└ dev      ← Vercel Preview 자동 배포. 평소 작업은 여기
  └ feat/* ← 마일스톤 단위 브랜치 (feat/m4-specimen-view)
```

- 커밋 단위는 마일스톤 하위 항목 1개. 메시지는 한국어 허용.
- **전시 직전에는 태그를 찍는다** (`v1.0-exhibition`). 현장에서 뭘 잘못 건드려도 되돌릴 지점.
- `.env` 는 커밋 금지, `.env.example` 만 커밋. Vercel 환경변수는 대시보드에서 설정.
- `bridge/` 는 Vercel 배포에 포함되지 않는다 (`.vercelignore`).

---

## 14. 전시 현장 모드

와이파이가 없을 수 있으므로:

1. `npm run build` → `dist/` 를 전시 PC에 통째로 복사
2. `npx serve dist` 로 로컬 서버 기동
   (파일 프로토콜 직접 열기는 라우팅이 깨짐 → **반드시 로컬 서버**)
3. `node bridge/index.js` 로 브릿지 기동
4. 브라우저 키오스크 모드 `--kiosk --app=http://localhost:3000`
5. URL 파라미터 프리셋: `?exhibition=1` → 커서 자동 숨김, 시뮬 자동 재생, 무입력 3분 후 개괄 상태 복귀

2~4를 묶은 `start-exhibition.bat` 한 개를 M11에서 만든다. 현장에서 더블클릭 하나로 끝나게.

---

## 15. 아직 결정 안 한 것 (가이드 필요)

시작하는 데 지장은 없고, 해당 마일스톤 전까지 답이 있으면 됩니다.

| # | 질문 | 필요 시점 |
|---|---|---|
| 1 | 계통(taxon) A/B/C의 실제 이름과 개수는? | M2 |
| 2 | 실제 전자생물이 총 몇 종인가? (목차 스트립 스크롤 여부를 가름) | M3 |
| 3 | 표본 비주얼의 화면비 — 정사각/세로형/원본 유지? 배경 제거된 누끼인가? | M4 |
| 4 | 개체당 주석 개수는 몇 개로 통일할까? (3~5개 권장) | M4 |
| 5 | '최초의 목적' 종류를 몇 가지로 둘까? (지금은 5개로 가정) | M6 |
| 6 | 시뮬레이션은 전 개체가 한 세계인가, 개체별 개별 세계인가? | M6 |
| 7 | **목업 하드웨어가 무엇인가?** (아두이노? 센서 종류? 슬롯이 물리적으로 몇 개?) | M9 |
| 8 | **관람객의 물리 조작이 "표본 선택"인가 "환경 조작"인가, 둘 다인가?** | M9 |
| 9 | 전시 화면 해상도/비율, 설치 방향 (가로/세로) | M11 |

7·8번이 가장 중요합니다. 프로토콜(§7-2)은 지금 **가정으로 써둔 것**이라,
실제 목업 형태가 정해지면 이 표를 먼저 고치고 나머지를 맞춥니다.

---

## 부록 A. 지금 바로 시작할 명령

```bash
npm install        # 의존성: react, react-dom 뿐 (+ M10에서 @supabase/supabase-js)
npm run dev        # 개발 서버 — Node 24 OK
npm run build      # 빌드 — Node 20/22 전용 (debug.md #1)
```

**UI 라이브러리·CSS 프레임워크·상태관리·라우팅 라이브러리는 넣지 않는다.**
로우파이 와이어프레임에 필요 없고, 나중에 디자인 방향을 제약한다.
(브릿지용 `ws` / `serialport`는 M9에서 `bridge/` 안에 따로 설치)
